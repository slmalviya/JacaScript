let str = '  hello  ';
console.log(str);
console.log(str.trim()); // for trim the whiteSpace from to String like this and in this only front and back remove the whitespace not middle 
console.log(str.toUpperCase()); // for uppercase
console.log(str.toLowerCase());  // for lowercase 
let msg = 'IloveCoding';
console.log(msg.indexOf('C')); // find the first of index jese li C ka indez "5" haii.. 